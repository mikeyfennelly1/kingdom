= Kingdom — Secure Messaging Application
:toc: left
:toclevels: 2

image:https://img.shields.io/website?label=production&url=https%3A%2F%2Fupdakingdom.theburkenator.com%2Fhealth[Production Health,link=https://updakingdom.theburkenator.com/health]
image:https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fupdakingdom.theburkenator.com%2Fapi&query=%24.version&label=version[Version,link=https://updakingdom.theburkenator.com/api]

== Building and Running

=== Host Machine

1. Clones this repo.
2. Downloads nix 2.34.7.
3. Installs all link:flake.nix[dependencies defined in nix flake specs].
5. Builds the server (`kds`) and the Qt6 GUI client (`kdctl`).

[source, bash]
----
wget -qO- https://raw.githubusercontent.com/mikeyfennelly1/kingdom/main/scripts/init.sh | bash
----

== Running the application stack locally

Run the app.

[source, bash]
----
./scripts/start.sh
----

Run a health check.

[source,bash]
----
./scripts/health-check.sh --local --secure
----

== Using the GUI (kdctl)

`kdctl` is a Qt6 desktop application. Launch it with:

[source,bash]
----
kdctl
----

=== Login screen

On startup you will see a login screen with three fields:

* **Server URL** — the address of the `kds` server, e.g. `https://localhost:8080`
* **Username**
* **Password**

Use **Sign Up** to register a new account or **Log In** to authenticate with an existing one. On first signup the client generates your X25519 and Ed25519 keypairs and uploads the public key bundle to the server. Your private keys are encrypted at rest under a key derived from your password.

=== Messaging

After login you will see the main window:

* **Left panel** — your conversation list. Click **New Conversation** to start one with another user.
* **Right panel** — messages in the selected conversation. Type in the box at the bottom and click **Send**.

All messages are end-to-end encrypted (X3DH + XChaCha20-Poly1305) before being sent to the server. The server never sees plaintext.

=== Additional actions

Select a message in the list to enable:

* **Forward** — re-encrypts and sends the message to another user or conversation
* **Delete** — removes the message from the server (sender only)
* **Revoke** — revokes another participant's access to the message

The conversation list and messages refresh automatically on a timer.

== Blockchain Sidecar

The sidecar records message hashes on the Ethereum Sepolia testnet. It runs as the `kd-sidecar` Docker Compose service alongside `kds` and is started automatically with the rest of the stack.

In production the required environment variables (`SEPOLIA_RPC_URL`, `PRIVATE_KEY`, `CONTRACT_ADDRESS`) are written to the VM's `.env` file by the Ansible deploy playbook. For local development, add them to your `.env` file manually:

[source]
----
SEPOLIA_RPC_URL=https://...
PRIVATE_KEY=0x...
CONTRACT_ADDRESS=0x8955A2361A978A41F721e58B2766b614768Db596
----

The sidecar starts with `docker compose up` and runs on port 3001. `kds` calls it automatically when messages are sent.

=== Verification page

The verification page is hosted at:

https://mikeyfennelly1.github.io/kingdom/

Paste a transaction hash from a sent message and the message ciphertext to verify on-chain integrity. The page runs entirely in the browser — no Kingdom backend required.

