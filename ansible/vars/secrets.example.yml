# Example secrets for Ansible deployment
# Rename this file to secrets.yml and fill in the values

postgres_user: "kingdom_user"
postgres_password: "change_me_to_a_secure_password"
postgres_db: "kingdom_db"
postgres_port: 5432

# Optional overrides
# kd_port: 8080
# kd_log_level: "info"
